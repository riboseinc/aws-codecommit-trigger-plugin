<?jelly escape-by-default='true'?>

<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form" xmlns:st="jelly:stapler">
    <f:entry field="${instance != null ? null : 'id'}" title="${%ID}">
        <f:textbox value="${instance != null ? instance.id : null}" readonly="readonly"/>
    </f:entry>
    <f:block>Deprecated credentials, you could go to Jenkins Config Page <a href="${rootURL}/configure">AWS Code Commit Trigger SQS Plugin</a> to do Migration steps</f:block>
</j:jelly>
